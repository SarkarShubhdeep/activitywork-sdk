{
  "name": "activitywork-sdk-monorepo",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "clean": "rm -rf packages/activitywork-sdk/dist",
    "build": "pnpm --filter ./packages/activitywork-sdk build",
    "lint": "pnpm --filter ./packages/activitywork-sdk lint",
    "typecheck": "pnpm --filter ./packages/activitywork-sdk typecheck",
    "test": "pnpm --filter ./packages/activitywork-sdk test"
  }
}
